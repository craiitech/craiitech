
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * Helper Functions
     */
    function isSignedIn() {
      return request.auth != null;
    }

    function hasProfile() {
      return isSignedIn() && exists(/databases/$(database)/documents/users/$(request.auth.uid));
    }

    function profile() {
      return get(/databases/$(database)/documents/users/$(request.auth.uid)).data;
    }

    /**
     * Role Hierarchy Checks
     */
    function isAdmin() {
      return isSignedIn() && (
        exists(/databases/$(database)/documents/roles_admin/$(request.auth.uid)) ||
        (hasProfile() && (profile().role.matches('(?i).*admin.*') || profile().roleId == 'admin'))
      );
    }

    function isAuditor() {
      return isSignedIn() && hasProfile() && profile().role.matches('(?i).*auditor.*');
    }

    function isSupervisor() {
      return isSignedIn() && hasProfile() && profile().role.matches('(?i)^(Campus Director|Campus ODIMO|Admin|Vice President.*|Director.*)$');
    }

    // --------------------------------------------------------------------------
    // GAD CORNER & PCW REPORTING
    // --------------------------------------------------------------------------
    
    match /gadInitiatives/{id} {
      allow read, list: if isSignedIn();
      allow create, update: if isSignedIn() && (isAdmin() || (hasProfile() && profile().role.matches('(?i).*(coordinator|director|odimo).*')));
      allow delete: if isAdmin();
    }

    match /gadMainstreaming/{id} {
      allow read, list: if isSignedIn();
      allow write: if isSignedIn() && (isAdmin() || (hasProfile() && profile().role.matches('(?i).*(coordinator|director|odimo).*')));
    }

    // --------------------------------------------------------------------------
    // UNIT FORMS & WORKFLOW
    // --------------------------------------------------------------------------
    
    match /unitFormRequests/{requestId} { 
      allow read, write: if isSignedIn();
    }

    match /unitForms/{formId} { 
      allow read, write: if isSignedIn();
    }

    match /formDownloadLogs/{logId} {
      allow read, write: if isSignedIn();
    }

    // --------------------------------------------------------------------------
    // CORE COLLECTIONS
    // --------------------------------------------------------------------------
    
    match /users/{userId} {
      allow read, get, list: if isSignedIn();
      allow create: if isSignedIn() && request.auth.uid == userId;
      allow update: if (isSignedIn() && request.auth.uid == userId) || isAdmin();
      allow delete: if isAdmin();
    }
    
    match /roles_admin/{userId} {
      allow read: if isSignedIn();
      allow write: if isAdmin();
    }

    match /submissions/{id} { allow read, list: if isSignedIn(); allow write: if isSignedIn(); }
    match /risks/{id} { allow read, list: if isSignedIn(); allow write: if isSignedIn(); }
    match /unitMonitoringRecords/{id} { allow read, list: if isSignedIn(); allow write: if isAdmin() || (isSupervisor() && profile().role == 'Campus ODIMO'); }
    match /auditSchedules/{id} { allow read, list: if isSignedIn(); allow write: if isAdmin() || isAuditor(); }
    match /auditFindings/{id} { allow read, list: if isSignedIn(); allow write: if isAdmin() || isAuditor(); }
    match /correctiveActionRequests/{carId} { allow read, list: if isSignedIn(); allow write: if isAdmin() || isAuditor() || isSupervisor(); }
    match /correctiveActionPlans/{id} { allow read, list: if isSignedIn(); allow write: if isSignedIn(); }
    match /academicPrograms/{id} { allow read, list: if isSignedIn(); allow write: if isAdmin() || isSupervisor(); }
    match /programCompliances/{id} { allow read, list: if isSignedIn(); allow write: if isSignedIn(); }
    
    match /campusSettings/{settingId} {
      allow read, list: if isSignedIn();
      allow write: if isSignedIn();
    }

    match /qaAdvisories/{advisoryId} { 
      allow read, list: if isSignedIn(); 
      allow write: if isAdmin(); 
    }

    match /managementReviewOutputs/{outputId} {
      allow read, list: if isSignedIn();
      allow write: if isAdmin() || isAuditor();
      allow update: if isSignedIn();
    }

    match /qaAuditReports/{reportId} {
      allow read, list: if isSignedIn();
      allow write: if isAdmin() || isAuditor();
    }

    match /managementReviews/{mrId} {
      allow read, list: if isSignedIn();
      allow write: if isAdmin() || isAuditor();
    }

    match /campuses/{id} { allow read, list: if isSignedIn(); allow write: if isAdmin(); }
    match /units/{id} { allow read, list: if isSignedIn(); allow write: if isAdmin(); }
    match /roles/{id} { allow read, list: if isSignedIn(); allow write: if isAdmin(); }
    match /cycles/{id} { allow read, list: if isSignedIn(); allow write: if isAdmin(); }
    match /isoClauses/{id} { allow read, list: if isSignedIn(); allow write: if isAdmin(); }
    match /procedureManuals/{id} { allow read, list: if isSignedIn(); allow write: if isAdmin(); }
    match /eomsPolicyManuals/{id} { allow read, list: if isSignedIn(); allow write: if isAdmin(); }

    match /system/signatories {
      allow read: if isSignedIn();
      allow write: if isAdmin();
    }

    match /activityLogs/{logId} {
      allow read: if isAdmin();
      allow create: if isSignedIn();
    }

    match /errorReports/{reportId} {
      allow read: if isAdmin();
      allow create: if isSignedIn();
      allow update: if isAdmin();
    }

    match /appFeedbacks/{feedbackId} {
      allow read: if isAdmin();
      allow create: if isSignedIn();
    }

    match /softwareEvaluations/{evalId} {
      allow read: if isAdmin() || isAuditor();
      allow create: if true; 
    }

    match /{path=**} {
      allow read, write: if isAdmin();
    }
  }
}
